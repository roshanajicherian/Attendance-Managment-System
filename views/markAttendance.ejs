<%- include("header") %>
<div class="container-fluid">
    <div class="centerAlign">
        <h1>Mark Attendance</h1>
        <form action="/markAttendance" method="post">
            <label class="mt-3" for="semSelect">Semester</label>
            <select class="mt-3" name="semSelect" id="semSelect">
                <option value="S1">S1</option>
                <option value="S2">S2</option>
                <option value="S3">S3</option>
                <option value="S4">S4</option>
                <option value="S5">S5</option>
                <option value="S6">S6</option>
                <option value="S7">S7</option>
                <option value="S8">S8</option>
            </select>
            <!-- TODO add condition over here -->
            <label class="mt-3 ml-3" for="courseSelect">Select Course</label>
            <select class="mt-3" name="courseSelect" id="courseSelect">
                <% for(let i = 0; i < courseList.length; i++) { %>
                    <option value="<%= courseList[i].cid %>"><%= courseList[i].cName %></option>
                <% } %>
            </select>
            <label class="mt-3 ml-3" for="dateSelect">Select Date</label>
            <input type="date" id="dateSelect" name="dateSelect">
            <br>
            <input type="submit" value="Confirm" class="btn btn-success mt-3">
            <a href="/teacherLanding" class="btn btn-danger mt-3">Cancel</a>
        </form>
    </div>
    <form action="/markAttendanceConfirm" method="post">
        <div class="studentDisplayTable">
            <table class="table mt-2 table-bordered table-sm">
                <thead class="thead-dark">
                    <tr>
                        <td>Student ID</td>
                        <td>Student Name</td>
                        <td>Present/Absent</td>
                    </tr>
                </thead>
                <tbody class="table-striped">
                    <% if(studentDetails) { %>
                        <% for(let i = 0; i < studentDetails.length; i++) { %>
                            <tr>
                                <td><%= studentDetails[i].sId %></td>
                                <td><%= studentDetails[i].sName %></td>
                                <td><input type="checkbox" name="studentDetails[]" value="<%= studentDetails[i].sId %>"></td>
                            </tr>
                        <% } %>
                    <% } %>
                </tbody>
            </table>
        </div>
        <button class="btn btn-success" type="submit">Confirm</button>
    </form>
</div>
<script src="js/main.js"></script>
<%- include("footer") %>